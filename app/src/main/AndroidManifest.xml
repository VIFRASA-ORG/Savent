<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vitandreasorino.savent">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Savent">
        <activity android:name=".SelectPatientActivity" />
        <activity
            android:name=".GruppiTab.CreazioneGruppo.AddContacts"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".GruppiTab.CreazioneGruppo.AddGroup" />
        <activity android:name=".GruppiTab.GroupDetailActivity" />
        <activity android:name=".EventiTab.CreazioneEvento.MapActivity" />
        <activity android:name=".EventiTab.CreazioneEvento.NewEvent" />
        <activity android:name=".AccountTab.ChangeCredentialAccountActivity" />
        <activity android:name=".EventiTab.EventDetailActivity" />
        <activity android:name=".Registrazioni.RegistrazioniEnte.RegisterEnteActivity" />
        <activity android:name=".Registrazioni.RegisterActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogSingInActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".HomeActivityEnte"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>

</manifest>